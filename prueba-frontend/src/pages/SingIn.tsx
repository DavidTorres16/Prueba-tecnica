import { SingInForm } from '@components/layout/SingInForm'
import { Banner } from '@components/layout/banner'

export const SingIn = () => {
    return (
        <main>
            <Banner/>
            <SingInForm/>
        </main>
    )
}
